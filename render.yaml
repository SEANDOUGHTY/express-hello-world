services:
- type: web
  name: service
  env: node
  buildCommand: yarn
  startCommand: node app.js
  region: oregon
  plan: starter
  autoDeploy: false
  envVars:
  - key: DB_URL
    fromDatabase:
       name: elephant
       property: connectionString

databases:
- name: elephant
  databaseName: mydb 
